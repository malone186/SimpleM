# OCR 자체 모델 정량 평가 지표 — Qwen3.5-0.8B vs Qwen3-VL-2B

- **평가 세트**: 실제 영수증 130장 (품목 570개), `data/receipt774/test.jsonl`
- **환경**: RTX 5060, llama.cpp
- **출처**: 2026-07 평가 아카이브(`backend/vlm_finetune/metrics/`)의 보존된 이미지별 pred/gt를
  2026-08-12 재채점(`rescore_metrics.py`)한 실측값. 모델 재실행 없음.
- **결과 파일**: `backend/vlm_finetune/metrics/rescore_extended.json`

## Qwen3.5-0.8B (v2 파인튜닝, llama.cpp Q8 — 서빙 대표 구성)

| 지표 | 값 | 비고 |
|---|---|---|
| **CER** (품목명) | **0.307** | 파인튜닝 전 베이스 0.902 → 1/3로 감소 |
| **Field Accuracy** | **평균 0.651** | name 0.368 · quantity 0.747 · unit_price 0.744 · amount 0.746 |
| **Exact Match** (품목, 4필드 micro) | **0.358** | name+qty+unit_price+amount 전부 일치 |
| **F1-Score** (품목명) | **0.500** | 기존 평가값 |
| **Complete Receipt Accuracy** | **0.246** | 품목 수 일치 + 전 품목 필드 일치 영수증 비율 |
| **Inference Time** | **3.80 s/장** | 콜드 로드 포함 (웜 평균 3.35s) |

## Qwen3-VL-2B (파인튜닝, Q4 @1024)

| 지표 | 값 | 비고 |
|---|---|---|
| **CER** | — | pred/gt 미보존, GPU 재실행 필요 |
| **Field Accuracy** | — | 상동 |
| **Exact Match** (품목, 3필드 macro) | **0.634** | name+qty+amount 기준 (같은 기준 0.8B는 0.493) |
| **F1-Score** (품목명) | **0.666** | |
| **Complete Receipt Accuracy** | **0.354** | 0.8B와 같은 3필드 기준 — 직접 비교 가능 |
| **Inference Time** | **4.62 s/장** | |

## 두 모델 직접 비교 (양쪽 다 있는 지표만)

| 지표 | 0.8B | 2B | 우세 |
|---|---|---|---|
| Exact Match (품목 3필드) | 0.493 | **0.634** | 2B |
| F1-Score (품목명) | 0.500 | **0.666** | 2B |
| Complete Receipt Accuracy | 0.246 | **0.354** | 2B |
| Inference Time | **3.80 s** | 4.62 s | 0.8B |

→ 정확도 전 지표 2B 우세, 속도만 0.8B 우세. 그러나 둘 다 로컬 GPU 서빙 전제 +
정확도가 Gemini API에 못 미쳐 **Gemini API로 전환** (운영 현재 상태).

## 지표 정의

- **CER**: gt 품목과 pred 품목을 짝지은 뒤(정규화 이름 완전일치 → 남은 것은 편집거리
  최소 그리디) 편집거리 합 ÷ gt 품목명 총 글자수. 짝 없는 gt 품목은 전체 삭제로 계산,
  환각 품목은 CER 제외(precision/F1이 담당). 이름 정규화 = 공백 제거 + 소문자화.
- **Field Accuracy**: 짝지어진 품목에서 필드별(name/quantity/unit_price/amount) 일치 비율.
  분모는 gt 품목 570개(micro), 숫자 필드는 ±0.5 허용.
- **Exact Match**: 한 품목의 대상 필드가 전부 일치. 4필드(micro)와 3필드(이미지별 macro,
  기존 full recall) 두 가지 — 2B는 3필드만 산출 가능.
- **F1-Score**: 품목명 recall/precision 조화평균 (이미지별 macro, 기존 평가값).
- **Complete Receipt Accuracy**: 품목 수 일치 + 전 품목 name·qty·amount 일치인 영수증
  비율 (0.8B는 4필드로 조여도 0.246 동일).
- **Inference Time**: 영수증 1장당 평균 추론 시간.

## 인용 주의

- 기존 표(recall/F1)는 **이미지별 macro**, 확장 지표(필드 정확도·4필드 Exact Match)는
  **570품목 micro**. name이 macro 0.500 vs micro 0.368로 갈리는 건 오류가 품목 많은
  영수증에 몰려 있어서다 — 한 표에 섞어 쓰지 말고 기준을 명시할 것.
- 합성데이터 초기 평가(필드 정확도 0.9~1.0, n=60)는 실영수증보다 훨씬 쉬움 —
  이 문서의 실영수증 수치와 섞어 인용 금지.
