# {$API_DOMAIN}은 docker-compose.prod.yml의 environment(API_DOMAIN)에서 주입된다.
# 도메인의 A 레코드가 이 서버 IP를 가리키고 80/443이 열려 있으면 인증서는 자동 발급된다.
{$API_DOMAIN} {
	reverse_proxy api:8000
	encode gzip

	# OCR 이미지 업로드가 클 수 있어 본문 제한을 넉넉히
	request_body {
		max_size 20MB
	}
}
