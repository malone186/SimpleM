# 발표자료 제작 브리핑 — 사용자 트래킹 & 관리자 분석 콘솔

> **이 문서의 용도**: 발표 슬라이드 제작을 위한 원본 자료입니다.
> 아래 내용을 바탕으로 발표 슬라이드(또는 아티팩트)를 만들어 주세요.
> 문서 자체가 슬라이드가 아니라, **슬라이드를 만들기 위한 재료·수치·서사**입니다.

---

## 0. 한 줄 요약 (발표 핵심 메시지)

> **"관리자가 '누가 어떻게 들어와서, 얼마나 쓰고, 언제 떠나는지'를 볼 수 있게 만들었다.
> 그것도 출시 후에 코드를 다시 안 고쳐도 되는 구조로."**

---

## 1. 프로젝트 배경

| 항목 | 내용 |
|---|---|
| 서비스명 | **SimpleM / 브루노트** — 카페 사장님을 위한 통합 운영 플랫폼 |
| 팀 구성 | 5인 (백엔드 3명 A·B·C + 프론트엔드 2명) |
| 발표자 역할 | **백엔드 B** — AI 오케스트레이션·챗봇·OCR·리포트·문서 자동화 |
| 기술 스택 | FastAPI · SQLAlchemy · PostgreSQL(Neon) / React Native(Expo) / 관리자 콘솔(Vanilla JS) |
| 기존 기능 | 재고·발주, 운영(급여·세무·예측), AI 챗봇, OCR 영수증 인식, 관리자 콘솔 |

**문제의식**: 관리자 콘솔에 회원수·결제·CS 지표는 있었지만,
**"이 사장님이 어떤 경로로 들어왔는가"**, **"들어와서 실제로 쓰고 있는가"** 를 알 수 없었다.
→ 마케팅 예산 배분도, 이탈 대응도 감으로 할 수밖에 없는 상태.

---

## 2. 만든 것 — 기능 2가지

### 기능 ①: 유입 경로(Acquisition) 트래킹

**"이 사장님은 어떤 경로로 우리 서비스를 알게 됐나"를 가입 시점에 1건 수집(first-touch)**

- **수집 지점**: Expo 회원가입 2단계에 "어떻게 알게 되셨나요? (선택)" 칩 7개 추가
- **채널 분류 체계** (저장은 정규 키, 표시는 한글 라벨)

| 정규 키 | 표시 라벨 |
|---|---|
| `referral` | 지인 추천 |
| `web_search` | 포털/구글 검색 |
| `instagram` | 인스타그램 |
| `app_store` | 앱스토어 검색 |
| `youtube` | 유튜브 |
| `naver_blog` | 네이버 블로그/카페 |
| `etc` | 기타 |

- **저장**: `users` 테이블에 `acquisition_source`, `acquisition_detail`, `acquisition_at` (전부 nullable)
- **정규화**: 분류표에 없는 값 → `etc` / 값 없음 → `NULL`
- **집계 API**: `GET /admin/dashboard/acquisition`
- **관리자 화면**: SVG 도넛 차트 + 채널별 막대(건수·비율)

**핵심 설계 — 실측/추정 하이브리드**
실데이터가 없는 초기에도 대시보드가 비지 않도록, 유입값이 없는 회원은
`user.id` 해시 기반 **결정적(deterministic) 가중 배정**으로 채운다.
- 같은 id → 항상 같은 채널 (새로고침해도 안 흔들림)
- 원본 DB를 오염시키지 않음 (집계 시점에만 배정)
- 응답에 **`seeded_count`** 를 실어 "몇 명이 추정값인지" **투명하게 표시**
- 실제 데이터가 들어오면 **실값이 항상 우선** → 자동으로 실측으로 대체

---

### 기능 ②: 활동·리텐션(Activity/Retention) 트래킹

**"들어와서 실제로 쓰고 있는가, 떠나려 하는가"를 서버 로그로 자동 수집**

- **신규 테이블** `tracking_events`
  | 컬럼 | 의미 |
  |---|---|
  | `email` | 누가 (JWT `sub`에서 추출, 없으면 익명) |
  | `method` / `path` | 무엇을 |
  | `feature` | 기능 카테고리 (재고/운영/챗봇/인증/법률/센서/원두검색/문의/기타) |
  | `status_code` | 응답 결과 |
  | `created_at` | 언제 (인덱스) |

- **수집 방식**: FastAPI **미들웨어 1개**가 모든 `/api/v1` 인증 요청을 자동 기록
  - 관리자 콘솔 트래픽(`/admin`)·CORS 프리플라이트는 제외
  - **전 구간 try/except** → 트래킹 실패가 절대 원 요청을 막지 않음
  - **프론트엔드 계측 코드 0줄**

- **집계 API**: `GET /admin/dashboard/activity`

- **관리자 화면 구성**
  - 상단 지표 타일 5개: 오늘 활성 / 이번 주 활성 / 이번 달 활성 / 누적 이벤트 / **이탈 위험**(강조색)
  - 좌: **기능별 사용량** 막대 (어떤 기능이 실제로 쓰이는가)
  - 우: **이탈 위험 회원** 리스트 (7일+ 미접속, "접속 이력 없음"은 최상단)

**핵심 설계 — 이벤트 로그가 상위 지표를 흡수**
별도 컬럼을 만들지 않고 로그 집계로 파생시켰다.
- `마지막 접속` = 그 회원의 `MAX(created_at)`
- `활성화 시점` = 특정 이벤트의 첫 발생
- `이탈 위험` = 마지막 활동이 7일 이상 지났거나 활동 이력이 아예 없음

---

## 3. 기술적 의사결정 (발표에서 가장 어필할 부분)

### 결정 1. "데모용"과 "출시용"을 분리하지 않았다

| | 선택지 A | 선택지 B (채택) |
|---|---|---|
| 방식 | 데모용 더미로 만들고 출시 때 새로 개발 | **응답 인터페이스를 실데이터 기준으로 먼저 고정** |
| 결과 | 두 번 개발, 화면 코드 폐기 | 데이터 소스만 교체, **화면 코드 100% 재사용** |

→ 출시 시 `_resolve_channel()` 한 함수만 "계산 → 컬럼 읽기"로 바뀌고, API 응답 형태와 관리자 화면은 그대로.

### 결정 2. 프론트 계측 대신 **서버 미들웨어**

| | 프론트 화면마다 이벤트 심기 | **백엔드 미들웨어 1개 (채택)** |
|---|---|---|
| 수정 범위 | 앱 화면 수십 곳 (여러 팀원 소유 파일) | **main.py 한 블록** |
| 커버리지 | 심은 곳만 | **모든 API 요청 자동** |
| 팀 협업 비용 | 프론트 2명과 조율 필요 | 없음 |

→ 5인 팀에서 **남의 파일을 안 건드리고 전면 커버리지**를 얻는 선택.

### 결정 3. 기존 테이블 ALTER 대신 **신규 테이블**

공유 DB의 `users` 테이블은 앱 계정이 **소유자가 아니라 컬럼 추가 불가**(권한 거부).
반면 `tracking_events`는 **내가 새로 만드는 테이블 → 소유권 확보** → 권한 벽을 구조적으로 우회.

### 결정 4. 팀 소유권 규칙 준수 + 단계 분리

팀 규칙상 타 팀원 소유 파일은 수정 금지.
기능을 **Phase 1(내 소유 파일만으로 완결) → Phase 2(협업 필요)** 로 쪼개서,
협업 대기 없이 먼저 동작하는 결과물을 만들고 이후 실수집을 붙였다.

---

## 4. 트러블슈팅 사례 (실전 경험 어필용)

> 발표에서 "그냥 만들었다"가 아니라 **"막힌 걸 어떻게 뚫었나"** 를 보여주는 재료.

### ① 커밋된 Git 충돌 마커로 백엔드 전체 기동 불가
- **증상**: 서버가 `SyntaxError`로 아예 안 뜸
- **원인**: `git stash pop` 충돌 마커(`<<<<<<< Updated upstream`)가 **해소되지 않은 채 커밋**되어 있었음
- **해결**: 충돌 양쪽이 서로 다른 엔드포인트(원두 큐레이터 vs 알바생 CRUD)임을 확인 → **양쪽 다 보존**하고 마커만 제거
- **검증**: 두 엔드포인트 모두 200 응답 확인

### ② 공유 DB 권한 벽 → 근본 해결(클라우드 DB 이전)
- **증상**: 로그인 등 모든 회원 관련 API가 500
- **원인 추적**: `InsufficientPrivilege: users 테이블의 소유주여야만 합니다` → 컬럼 추가 실패 → 모델은 컬럼을 선언 → 모든 `User` 쿼리가 `UndefinedColumn`
- **해결**: 소유권이 있는 **Neon 클라우드 PostgreSQL로 전환** → 테이블/컬럼 자동 생성, 기능 전체 정상화
- **교훈**: 권한 없는 공유 자원에 의존하면 기능 자체가 막힌다 → 소유권 있는 인프라 확보

### ③ 푸시 거부(non-fast-forward) — 움직이는 원격
- **증상**: `! [rejected] main -> main (non-fast-forward)`
- **원인**: 팀원이 21개 + 작업 중 1개를 추가로 push (원격이 계속 앞서감)
- **해결**: 병합 전 **`merge-tree`로 충돌을 미리 예측**(워킹트리 무변경) → 실제 충돌 2파일만 양쪽 보존 해소 → 통합 후 push 성공

### ④ 프론트 번들 500 + MIME 에러 (2회 반복)
- **증상**: `Refused to execute script... MIME type ('application/json')`
- **원인**: pull 후 팀원이 추가한 새 의존성 미설치 (`react-native-webview`, `react-native-ble-plx`, `expo-haptics`)
- **해결**: `npm install` + 번들러 재시작 → **"pull 후엔 의존성 설치"** 를 팀 습관으로 정리

### ⑤ 관리자 콘솔 로그인 불가
- **원인**: 병합으로 새로 들어온 로그인 게이트의 `ADMIN_PASSWORD` 환경변수 미설정 (DB 문제로 오인하기 쉬움)
- **해결**: `.env`에 관리자 계정 설정 추가 → 원인이 DB가 아님을 로그로 규명

### ⑥ 한글 환경 인코딩 오류
- **원인**: 관리자 콘솔 실행 스크립트의 이모지가 Windows 기본 인코딩(cp949)에서 `UnicodeEncodeError`
- **해결**: UTF-8 강제 실행으로 우회 (원본 파일 무수정)

---

## 5. 발표용 수치 데이터 (차트 재료)

### 유입 경로 분포 (데모 시연 시점)
| 채널 | 인원 | 비율 |
|---|---|---|
| 지인 추천 | 1명 | 33.3% |
| 유튜브 | 1명 | 33.3% |
| 기타 | 1명 | 33.3% |
- 전체 3명 중 **1명만 추정값**(seeded), 2명은 가입 시 실수집

### 초기 대량 데이터 시점 (회원 7명)
| 채널 | 인원 | 비율 |
|---|---|---|
| 지인 추천 | 2명 | 28.6% |
| 포털/구글 검색 | 2명 | 28.6% |
| 인스타그램 | 2명 | 28.6% |
| 기타 | 1명 | 14.3% |

### 활동·리텐션 지표
| 지표 | 값 |
|---|---|
| 오늘 / 이번 주 / 이번 달 활성 회원 | 각 1명 |
| 누적 활동 이벤트 | 6건 |
| **이탈 위험 회원** | **2명** (가입 후 접속 이력 없음) |

### 기능별 사용량
| 기능 | 호출 |
|---|---|
| 운영 | 2건 |
| 재고 | 2건 |
| 인증 | 1건 |
| 챗봇 | 1건 |

---

## 6. 화면 구성 (슬라이드에 그릴 UI)

**관리자 콘솔 대시보드** — 커피 톤 디자인(에스프레소 브라운 `#3E291F`, 모카 `#7A5C4D`, 그린 `#4E7D3A`, 오렌지 `#C07030`, 배경 `#F5F2EC`)

기존 화면 아래에 **분석 카드 2개를 신규 추가**:

```
┌──────────────────────────────────────────────┐
│  🔀 사장님 유입 경로 분석            [LIVE]  │
│                                              │
│    ╭───╮      지인 추천   ▓▓▓▓▓  1명·33.3%   │
│   │  3  │     유튜브      ▓▓▓▓▓  1명·33.3%   │
│    ╰───╯      기타        ▓▓▓    1명·33.3%   │
│   전체 회원                                   │
│ ─────────────────────────────────────────────│
│ ℹ 전체 3명 중 1명은 추정값. 실데이터가       │
│   쌓이면 자동으로 실측값으로 대체됩니다.      │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│  📊 활동 · 리텐션 분석               [LIVE]  │
│  ┌────┐┌────┐┌────┐┌────┐┌────────┐         │
│  │오늘││이번││이번││누적││이탈위험 │         │
│  │ 1명││주1 ││달1 ││ 6건││  2명 ⚠ │         │
│  └────┘└────┘└────┘└────┘└────────┘         │
│  기능별 사용량        │  이탈 위험 회원       │
│  운영  ▓▓▓▓▓ 2건     │  ○ YoutubeCafe       │
│  재고  ▓▓▓▓▓ 2건     │    접속 이력 없음     │
│  인증  ▓▓    1건     │  ○ JunkCafe          │
│  챗봇  ▓▓    1건     │    접속 이력 없음     │
└──────────────────────────────────────────────┘
```

---

## 7. 검증 방법 (품질 어필)

기능을 "만들었다"에서 끝내지 않고 실제로 증명:
- **E2E 검증**: 실제 가입 API로 유입값 전송 → DB 저장 확인 → 집계 API 반영 확인 → `seeded_count` 감소 확인
- **정규화 검증**: 잘못된 채널값(`blahblah`) 전송 → `etc`로 자동 정규화 확인
- **회귀 검증**: 충돌 해소한 두 엔드포인트 200 응답 확인
- **렌더링 검증**: 헤드리스 브라우저로 실제 화면 캡처하여 차트·수치 표시 확인
- **정적 검증**: Python AST 파싱 / JS 문법 검사

---

## 8. 제안하는 슬라이드 구성

| # | 슬라이드 | 핵심 내용 |
|---|---|---|
| 1 | 표지 | 브루노트 — 사용자 트래킹 & 관리자 분석 |
| 2 | 문제 정의 | "회원은 있는데, 어디서 왔고 쓰고 있는지 모른다" |
| 3 | 해결 개요 | 유입 경로 + 활동·리텐션 두 축 |
| 4 | 기능 ① 유입 경로 | 가입 화면 칩 → DB → 도넛 차트 흐름도 |
| 5 | 기능 ① 화면 | 관리자 콘솔 유입 경로 카드 |
| 6 | 기능 ② 활동·리텐션 | 미들웨어 자동 수집 아키텍처 |
| 7 | 기능 ② 화면 | 활동 지표 + 기능별 사용량 + 이탈 위험 |
| 8 | **설계 포인트 1** | "출시 후 코드 수정 0" — 인터페이스 고정, 소스만 교체 |
| 9 | **설계 포인트 2** | 프론트 계측 0줄 — 서버 미들웨어 하나로 전면 커버 |
| 10 | **설계 포인트 3** | 실측/추정 하이브리드 + `seeded_count` 투명성 |
| 11 | **트러블슈팅** | 권한 벽 → 클라우드 DB 이전 (가장 임팩트 있는 사례) |
| 12 | **트러블슈팅** | 커밋된 충돌 마커 / 푸시 거부 / 의존성 |
| 13 | 검증 | E2E·정규화·회귀·렌더링 검증 |
| 14 | 확장 계획 | 일별 활성 추이, 챗봇 도구별 랭킹, 전환 퍼널, 코호트 |
| 15 | 마무리 | 한 줄 요약 재강조 |

---

## 9. 어필 포인트 (심사·질문 대비)

**강조할 것**
1. **미래 비용을 줄이는 설계** — 데모/출시를 따로 만들지 않고 인터페이스를 먼저 고정
2. **협업 제약을 설계로 푼 것** — 5인 팀 소유권 규칙 안에서 남의 파일 안 건드리고 전면 커버리지 달성
3. **데이터 정직성** — 추정값을 숨기지 않고 `seeded_count`로 노출 (신뢰할 수 있는 대시보드)
4. **장애 격리** — 트래킹 실패가 서비스(회원가입·API)를 절대 막지 않도록 전 구간 방어
5. **근본 원인 추적력** — "로그인 안 됨"에서 DB 권한 문제까지 파고들어 인프라 이전으로 해결

**예상 질문 & 답변**
- *"추정값을 넣는 건 데이터 조작 아닌가?"*
  → 실데이터가 항상 우선이고, 추정 인원수를 `seeded_count`로 화면에 명시합니다. 실데이터가 쌓이면 자동 대체됩니다.
- *"모든 요청을 DB에 쓰면 성능 부담은?"*
  → 현재는 요청당 1 insert. 트래픽이 커지면 비동기 큐/배치 삽입, 샘플링, 보관 주기(TTL) 적용이 다음 단계입니다.
- *"개인정보 이슈는?"*
  → 식별자는 이메일과 요청 경로 수준이며, 별도 수집 동의·보관 기간 정책과 함께 운영되어야 합니다.

---

## 10. 향후 확장 (로드맵 슬라이드용)

- **일별 활성 추이 그래프** (DAU 라인 차트)
- **챗봇 도구별 사용 랭킹** — AI 오케스트레이션이 강점이므로 "어떤 전문가/도구가 실제로 쓰이나"
- **전환 퍼널** — 가입 → 첫 기능 사용 → 프리미엄 전환
- **채널별 리텐션 비교** — "인스타 유입은 잘 남는가, 지인 추천이 잘 남는가"
- **지역·상권별 분포** — 가입 시 이미 받고 있으나 저장하지 않는 값 활용

---

## 11. 디자인 가이드 (슬라이드 톤)

- **컬러**: 에스프레소 브라운 `#3E291F` / 모카 `#7A5C4D` / 크림 `#F2EBE3` / 배경 `#F5F2EC` / 포인트 그린 `#4E7D3A` / 포인트 오렌지 `#C07030`
- **톤**: 카페 브랜드에 맞는 따뜻한 베이지·브라운 계열, 과한 채도 지양
- **강조**: 이탈 위험·경고는 오렌지 계열, 정상·성공은 그린 계열
- **차트**: 도넛(유입 분포) + 가로 막대(기능별 사용량) 조합
