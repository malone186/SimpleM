# 경영 리포트 고도화 — 프론트 연동 스펙 (2026-08-05)

작성: 백엔드 B · 대상: 프론트 A (`components/dashboard/ManagementReportCard.tsx`)

백엔드만 바꿨습니다. **카드를 수정하지 않아도 지금 그대로 동작합니다** — 기존에 쓰던 필드는
이름·타입 모두 그대로 두었습니다. 아래는 새로 추가된 필드와, 화면에 붙이면 좋을 것들입니다.

엔드포인트는 그대로: `GET /chatbot/reports/management?period_type=daily|weekly|monthly`

---

## 1. 먼저 알아야 할 것 — 순이익 숫자의 의미가 바뀌었습니다

`profit.estimated_profit`은 **값의 계산 근거가 달라졌습니다.** 화면 코드는 안 바꿔도 되지만,
숫자가 예전과 다르게 나오는 게 버그가 아니라는 점만 알아두시면 됩니다.

- **예전**: `매출 − 확정한 OCR 매입문서 − 기타지출 − 인건비`
- **지금**: `매출 − 팔린 메뉴의 레시피 재료비 − 기타지출 − 인건비`

바꾼 이유는 예전 방식이 **명세서를 언제 촬영했는지에 손익이 좌우됐기 때문**입니다.
이번 주에 사진을 한 장도 안 찍으면 재료비가 0원이라 순이익이 매출 전액에 가깝게 부풀고,
밀린 명세서를 하루에 몰아 찍으면 그날 일간 리포트만 대형 적자로 찍혔습니다.

> 실측: 데모 매장 8월 월간 리포트가 **순이익 456,285원 → 151,921원**으로 정정됐습니다.
> 매입 문서 기준이 재료비를 30만원 넘게 누락하고 있었습니다.

레시피가 덜 등록된 매장(레시피가 걸린 매출이 60% 미만)에서는 원가율을 믿을 수 없으므로
**자동으로 예전 매입 기준으로 되돌아갑니다.** 어느 쪽으로 계산했는지는 `profit.basis`
(`"recipe"` | `"purchase"`)로 알 수 있습니다.

`profit`에 추가된 필드:

| 필드 | 뜻 |
|---|---|
| `basis` | `"recipe"`(레시피 원가) 또는 `"purchase"`(매입 문서) |
| `material_cost` | 재료비 — `basis`에 따라 레시피 원가 또는 매입액 |
| `cash_total_cost` | 실제로 나간 돈 (매입 + 지출 + 인건비) |
| `cash_balance` | `매출 − cash_total_cost` — 현금 관점 |

`estimated_profit`(손익)과 `cash_balance`(현금)를 나란히 두면 *"이익은 나는데 통장이 빈"*
상황(재료를 대량 매입한 주)을 구분해 볼 수 있습니다. 비용 팝업에 한 줄 넣기 좋습니다.

---

## 2. 새로 들어온 필드

### `content.cogs` — 재료비·원가율·로스

```ts
{
  theoretical: number;        // 판매원가 (팔린 잔 × 레시피 재료비)
  cost_ratio: number | null;  // 매출 대비 재료비 % — 카페 손익의 핵심 지표
  coverage_pct: number | null;// 레시피가 걸린 매출 비중 %
  reliable: boolean;          // coverage 60% 이상이면 true
  uncovered_menus: { menu: string; total: number }[];  // 레시피 없어 원가 못 잡은 메뉴 (상위 5)
  uncovered_count: number;
  worst_margin_menus: {       // 원가율 높은 순 상위 3 — "많이 팔리는데 안 남는" 메뉴
    menu: string; quantity: number; revenue: number; cost: number; cost_ratio: number | null;
  }[];
  loss_amount: number;        // 재고 실사에서 줄어든 금액 (폐기·흘림·계량 오차)
  loss_pct: number | null;
  loss_items: { name: string; amount: number }[];  // 상위 3
}
```

**붙일 곳 제안**: 지금 `💰 추정 수익 상세 분석` 팝업에 `추정 마진율` 한 줄이 있는데,
그 아래 `재료비 {cost_ratio}%` 를 넣으면 사장님이 실제로 보는 숫자가 됩니다.
`🧾 비용 세부 구성 내역` 팝업의 "재료비 및 매장 기타경비"는
`total_cost − labor` 로 역산 중인데, 이제 `cogs.theoretical`을 직접 쓰면 정확합니다.

`worst_margin_menus`는 `☕ 판매 잔수 & 인기 메뉴` 팝업의 판매 순위와 짝이 됩니다 —
"많이 팔린 순"과 "안 남는 순"을 나란히 두면 메뉴 판단이 한 화면에서 끝납니다.

> ⚠️ `uncovered_count > 0`이면 그만큼 재료비가 빠져 **이익이 실제보다 커 보입니다.**
> 숫자 옆에 주의 표시를 하거나, 레시피 등록(`Menu` 화면)으로 유도해 주시면 좋겠습니다.

### `content.rhythm` — 영업 리듬 (시간대·요일)

```ts
{
  peak_hours: { hour: number; total: number; cups: number }[];      // 매출 상위 3개 시간대
  negative_hours: {          // 그 시간 인건비 > 매출 — 사람은 있는데 손님이 없던 구간
    hour: number; sales: number; labor: number; gap: number;
  }[];
  best_weekday:  { weekday: string; avg_total: number; days: number } | null;
  worst_weekday: { weekday: string; avg_total: number; days: number } | null;
}
```

원자료도 함께 나갑니다 — 차트를 그리신다면 이쪽을 쓰세요.

- `content.sales.hourly`: `{ hour, total, cups }[]` — 시간대별 매출
- `content.sales.weekday`: `{ weekday(0=월), total, cups, days, avg_total }[]`
- `content.labor.hourly_cost`: `{ hour, cost }[]` — 같은 시간 축의 인건비

`sales.hourly`와 `labor.hourly_cost`를 같은 x축에 겹친 막대 하나면
*"오후 3시엔 인건비가 매출보다 많다"* 가 한눈에 보입니다. 지금 카드에 있는
`매출 대비 인건비 38%` 한 줄보다 행동으로 이어집니다.

> 요일 비교(`best_weekday`/`worst_weekday`)는 **같은 요일 관측이 2회 이상일 때만** 채웁니다.
> 주간 리포트는 각 요일이 1회뿐이라 항상 `null`이고, 월간도 월초엔 `null`입니다.
> 표본이 없을 때 침묵하는 게 맞다고 봐서 그렇게 뒀습니다 — **null 처리 꼭 부탁드립니다.**

### `content.outlook` — 다음 기간 전망 (`null` 가능)

```ts
{
  days: number;                        // 일간=1, 주간=7
  expected_revenue: number;
  expected_cups: number;
  expected_daily_avg: number;
  recent_daily_avg: number | null;
  change_pct: number | null;           // 최근 하루 평균 대비 %
  model: string;
  stale: boolean;                      // 예측이 갱신된 지 오래됨
  busiest: { date: string; weekday: string; revenue: number; cups: number;
             weather: string | null; temp_max: number | null; holiday: string | null } | null;
  order_recommendations: object[];     // 예측 기반 발주 추천 (상위 3)
}
```

**`null`인 경우가 많습니다** — 아래 세 가지 중 하나면 `null`입니다.

1. `period_type === "monthly"` (예측 지평이 최대 14일이라 '다음 달'을 말할 근거가 없음)
2. 판매 기록이 14일 미만이라 예측 자체가 안 열린 매장
3. 예측 캐시가 아직 안 만들어짐 — **리포트는 예측을 직접 계산하지 않고 캐시를 읽기만 합니다.**
   SARIMAX 적합 + 날씨 API가 수 초 걸리는데 이 리포트는 홈 화면이 열릴 때마다 백그라운드로
   재생성되기 때문입니다. 대시보드의 예측 카드가 자기 경로에서 캐시를 채우면 그때부터 붙습니다.

`change_pct`도 별도로 `null`일 수 있습니다 — 비교할 **완결된** 날이 3일 미만이면 생략합니다
(진행 중인 오늘을 섞으면 평균이 낮아져 전망이 늘 급증으로 보입니다).

### `content.ai_actions` — 구조화된 액션 ★ 여기가 핵심입니다

```ts
{
  title: string;     // 12자 안팎, 버튼/카드 제목
  evidence: string;  // 근거 숫자 한 문장
  action: string;    // 할 일 한 문장 (해요체)
  screen: "Menu" | "Cost" | "Inventory" | "Staff" | "Document" | "Marketing" | "SalesInput";
}[]
```

Gemini가 리포트 숫자만 근거로 **0~3건** 뽑습니다. 짚을 게 없으면 빈 배열입니다 (억지로 안 채웁니다).
`screen`은 `RootStackParamList`의 실제 화면 이름이라 `navigation.navigate(a.screen)`으로 바로 갑니다.
백엔드에서 화이트리스트 검증을 하므로 목록 밖 값은 절대 안 나갑니다.

실제 출력 예시:

```
[Menu]      카라멜 마끼아또 원가 점검
            근거: 카라멜 마끼아또의 재료비 비중이 38.6%로 가장 높게 나타났어요.
            제안: 레시피를 확인하거나 판매가를 조정해 보세요.
[Inventory] 재료 발주하기
            근거: 에티오피아 원두와 서울우유 등 주요 재료가 안전 재고보다 부족한 상태예요.
            제안: 부족한 재료들을 확인하고 서둘러 발주를 진행해 주세요.
[Staff]     영업 시간 조정
            근거: 밤 9시에는 매출이 없는데 인건비가 1만 원 발생하고 있어요.
            제안: 마감 시간을 조금 앞당기는 것을 고려해 보세요.
```

**붙일 곳 제안**: 지금 카드 맨 아래 CTA가 `품목별 상세 표 보여줘`(챗봇 프리필) 하나뿐입니다.
그 위에 액션 카드 2~3개를 쌓고 각각 해당 화면으로 보내면, 리포트가 *읽는 것*에서
*하는 것*으로 바뀝니다. 기존 챗봇 버튼은 그대로 두셔도 됩니다.

---

## 3. 기존 필드 — 전부 그대로입니다

카드가 현재 읽고 있는 것 중 **바뀌거나 사라진 필드는 없습니다.**

`sales.total` · `sales.change_pct` · `sales.cups` · `sales.top_menus` · `sales.prev_total` ·
`purchases.document_count` · `expenses.total` · `labor.shift_count` · `labor.estimated_cost` ·
`inventory.ingredient_count` · `inventory.low_stock` · `orders.open_count` ·
`compliance_alerts` · `highlights` · `note` · `ai_advice` · `profit.total_cost` ·
`profit.estimated_profit` · `profit.margin_pct`

신규 계정 판별(`isEmptyReport`)에 쓰는 필드도 모두 그대로라 그 로직도 손댈 필요 없습니다.

### `highlights` 변경점 (문자열 배열인 건 동일)

- **최대 7개로 제한**했습니다. 항목이 늘면서 10줄까지 쏟아져 정작 손댈 거리가 묻혔습니다.
- **첫 줄은 항상 매출**이고, 나머지는 중요도순입니다:
  돈이 새는 것(적자·레시피 미등록) → 손댈 것(빈 시간 인건비·원가율·로스·재고·만료 서류)
  → 상태 수치(순이익·전망·재료비·인건비) → 배경(요일·피크·베스트 메뉴)
- 카드의 `formatFriendlyText()` 치환 규칙 중 `안전재고 이하 재료 N종`,
  `갱신 임박·만료 서류 N건`, `추정 수지 적자` 패턴은 **지금 문구와 안 맞습니다.**
  현재 백엔드가 내보내는 문구는 이미 `재고 부족: …`, `갱신 임박 서류: …`, `적자 N원 — 비용 점검 필요`
  형태라 치환이 안 걸리고 그냥 통과합니다. 동작에 문제는 없지만 죽은 코드라 정리하셔도 됩니다.

---

## 4. 참고

- 검증: `backend/tests/test_management_report_cogs.py` (11개, 전부 통과).
  손익 기준 전환·폴백·시간대 배분·요일 표본 규칙을 고정해 뒀습니다.
- 백엔드 전체 테스트 374 통과. 실패 4건은 이 변경 이전부터 있던 것들입니다
  (`test_forecast_holidays_events` 2건 · `test_rewards_api` 공유상태 플레이크).
- 응답에 `ai_advice_hash` / `ai_advice_at` / `ai_advice_version`이 섞여 나옵니다 —
  조언 캐시 관리용 내부 키라 화면에서는 무시하시면 됩니다.
- 질문 있으시면 편하게 주세요. 필드 추가·형태 변경 다 가능합니다.
