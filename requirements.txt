# 웹 서버 개발을 위한 기본 도구들
fastapi>=0.111.0          # 안내 데스크 직원 (웹 API 프레임워크)
uvicorn>=0.30.1          # 웹 서버를 실행시켜 주는 모터 엔진

# 데이터베이스 연결 및 관리 도구들
sqlalchemy>=2.0.31       # 파이썬과 데이터베이스 사이의 통역사 (ORM)
psycopg2-binary         # PostgreSQL 데이터베이스와 실제로 대화하는 통신 부품 (버전 자동 선택)
alembic>=1.13.2          # 데이터베이스 설계도 패치/변경 기록 관리자

# 로그인 및 보안 설정 관련 도구들
python-dotenv>=1.0.1     # .env 파일에서 비밀 열쇠 정보를 읽어오는 도구
pyjwt>=2.8.0             # 암호화된 회원 출입증(JWT 토큰) 발급 도구
passlib[bcrypt]>=1.7.4   # 사용자의 비밀번호를 안전하게 암호화(해싱)해서 저장하는 도구
