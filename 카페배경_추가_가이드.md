# 게임 룸 '카페 배경' 사진 추가 가이드 (디자인 담당자용)

게임 룸(홈 우상단 🎮 버튼)의 배경 사진을 상점에서 사서 갈아끼우는 기능입니다.
**코드는 전부 준비돼 있어서, 사진 파일 1개 + require 한 줄만 추가하면 끝납니다.**

## 준비된 아이템 8종 (백엔드 카탈로그에 이미 등록됨)

| 아이템 id | 이름 | 컨셉 |
|---|---|---|
| `room_window` | 창가 자리 | 큰 창으로 햇살, 창밖 거리 풍경 |
| `room_terrace` | 야외 테라스 | 파라솔·화분·노천카페 |
| `room_night` | 밤의 카페 | 전구 불빛, 창밖 밤거리 |
| `room_blossom` | 벚꽃 시즌 | 창밖 가득 벚꽃, 분홍 톤 |
| `room_rainy` | 비 오는 날 | 빗방울 맺힌 창, 따뜻한 실내 |
| `room_winter` | 겨울 카페 | 트리, 창밖 눈 |
| `room_sunset` | 노을 루프탑 | 옥상 카페, 주황빛 하늘 |
| `room_plant` | 플랜테리어 | 식물 가득한 초록 인테리어 |

## 사진 규격

- **세로 비율 약 1:2** (권장 1024×2048 안팎 — 기존 배경이 871×1806)
- **중앙~하단은 비교적 비워 두기** — 그 자리에 마스코트가 서 있음
- 현재 배경(`frontend/assets/game/room_bg.jpg`)과 같은 따뜻한 일러스트 화풍, 글자 없음
- 용량 큰 PNG면 JPG(품질 85 내외)로 변환 권장

## 추가 방법 (사진 1장당 2단계)

1. 사진 파일을 아래 경로에 **아이템 id와 같은 이름**으로 저장:
   ```
   frontend/assets/game/rooms/room_window.jpg
   ```

2. `frontend/src/components/brew/roomBackgrounds.ts`를 열어 해당 줄의 **주석만 해제**:
   ```ts
   export const ROOM_BGS: Record<string, ImageSourcePropType> = {
     room_window: require('../../../assets/game/rooms/room_window.jpg'),  // ← 이 줄
     // room_terrace: ...  (사진 없는 건 주석 유지)
   };
   ```

이게 전부입니다. 이러면 자동으로:
- 상점에 **"카페 배경"** 섹션이 생기고 그 아이템이 노출됩니다 (사진 미등록 아이템은 자동으로 숨겨짐 — 반쪽 상태로 팔릴 일 없음)
- 썸네일은 실제 사진으로 표시됩니다
- 구매·착용하면 게임 룸 배경이 그 사진으로 바뀝니다 (보관함에서 교체·해제 가능)

## 확인 방법

1. 프론트 재시작: `cd frontend && npx expo start --web --clear` (에셋 추가 후엔 `--clear` 필수)
2. 상점 → "카페 배경" 섹션에서 구매 → 착용
3. 홈 우상단 🎮 → 게임 룸 배경이 바뀌었는지 확인

## 8종을 다 채운 뒤에는

`roomBackgrounds.ts`의 주석 8줄이 모두 해제되면 끝. 새 배경을 더 만들고 싶으면
백엔드 `backend/app/services/ai/reward_service.py`의 SHOP_ITEMS에 `slot: "room"`
아이템을 한 줄 추가하고 위 1~2단계를 반복하면 됩니다 (id만 서로 맞추면 됨).
